node_modules
lib
*.log
.DS_Store
coverage
*.tsbuildinfo
*.tgz

# Local API keys / model credentials. dsh loads `~/.dsh/.env` for runtime
# credentials; this protects any project-local `.env` a contributor might
# add during dev from being committed.
.env
.env.*
!.env.example
